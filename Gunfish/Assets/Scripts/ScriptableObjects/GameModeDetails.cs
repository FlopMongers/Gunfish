using UnityEngine;

[CreateAssetMenu(fileName = "GameModeDetails", menuName = "ScriptableObjects/Game Mode Details", order = 0)]
public class GameModeDetails : ScriptableObject {
    public Texture2D image;
}